#include <cs50.h>
#include <stdio.h>

int main(void)
{  
    int h = get_int("Please enter number from 1 to 8: ");
    printf("Height, %i\n", h);
    while (h<0 || h>8)
    {
        printf("Error\n");
    }
         for (int i=0; i<h; i++)
        {
             for (int j=0; j<=1; j++)
             {
                 printf("#\n");
             }
        printf(" \n");
        }
  }